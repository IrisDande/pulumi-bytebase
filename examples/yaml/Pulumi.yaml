name: provider-bytebase-native
runtime: yaml
plugins:
  providers:
    - name: bytebase
      path: ../../bin

resources:
  myRandomResource:
    type: bytebase:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
